import { IPolicy, Operation } from "./types";
export declare class CombinedPolicy implements IPolicy {
    private _all;
    constructor(...all: IPolicy[]);
    comply(path: string, operation: Operation): boolean;
}
export declare class ReadOnlyPolicy implements IPolicy {
    comply(path: string, operation: Operation): boolean;
}
export declare class ForceRootPolicy implements IPolicy {
    private _root;
    constructor(root: string);
    comply(path: string, operation: Operation): boolean;
}
export declare class AllowPolicy implements IPolicy {
    comply(path: string, operation: Operation): boolean;
}
export declare class DenyPolicy implements IPolicy {
    comply(path: string, operation: Operation): boolean;
}
